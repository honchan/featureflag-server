export enum FeatureRuleTypes {
  DEFAULT = 'DEFAULT',
  WHITELIST = 'WHITELIST',
  SPLIT = 'SPLIT',
  ONETIME = 'ONETIME',
}

export enum FeatureRulePriorities {
  DEFAULT = 1,
  WHITELIST = 2,
  SPLIT = 3,
  ONETIME = 3,
}